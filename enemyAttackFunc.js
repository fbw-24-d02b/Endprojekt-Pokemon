// export const enemyAttack = (enemy) => {
//   const enemyAttacks = enemy.attacks.map((attack) => attack.damage);
//   const randomAttack = Math.floor(Math.random() * enemy[enemyAttacks.length]);
//   return randomAttack;
// };
